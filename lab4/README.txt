Lab4 contains a total of 19 vhd files, one report, including four more registers that make up the pipeline: 
ifid, idex, exmem, memewb. Finally, the overall connection is made in the pipelinedcpu0.vhd file.

 pipeline_ tb is the testbench file of pipelinecpu.